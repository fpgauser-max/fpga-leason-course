title: FPGA leason
description: FPGA 相关内容的学习
template:
  repo: fpga-leason-template
  name: fpga-leason
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
